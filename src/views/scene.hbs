{{#>template}}
  <div class="scene">
    <div class="scene-img">
      {{#if imageUrl}}<img onerror="this.src='/android-chrome-512x512.png'" src={{imageUrl}}>{{/if}}
      <p class="scene-text {{sceneTextClasses}}">{{description}}</p>
    </div>
    <div class="achievements">
      {{#each achievements}}
        <div class="achievement">
          <img class="achievement-img" onerror="this.src='/android-chrome-512x512.png'" src={{lookup this 1}}>
          <div>
            <h3 class="achievement-title">Достижение получено</h3>
            <p class="achievement-description">{{lookup this 0}}</p>
          </div>
        </div>
      {{/each}}
    </div>
    <div class="actions">
      {{#each actions}}
        <a href="/scene/{{lookup this 0}}?root={{../root}}" class="action">{{lookup this 1}}</a>
      {{/each}}
    </div>
  </div>
{{/template}}
